{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show" style="margin: 25px">
    <h3>Profile</h3>
    <p>Your name: {{ user.fullname }}</p>
    <p>Your email: {{ user.email }}</p>
    <a class="btn btn-primary" href="{{ path('fos_user_profile_edit') }}">Edit Details</a>
</div>
